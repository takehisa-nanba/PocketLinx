# Binaries
plx
plx.exe
plx_linux
plx_new

# WSL rootfs and distro
images/
distro/

# Temporary run logs or go specific
*.log
vendor/
.gemini/

# Container runtime data (IMPORTANT: avoid pushing user container data)
# Note: WSL backend creates these inside the distro, but if they are mounted 
# or created in the workspace, we should ignore them.
containers/
var/
varlib*
